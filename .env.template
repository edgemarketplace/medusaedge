# Intake-to-implementation bridge. If INTAKE_WEBHOOK_URL is unset, submissions are recorded
# locally as JSONL for development/self-hosted fallback only. Use Supabase, Airtable,
# a custom worker, or Hermes webhook for durable production intake storage.
INTAKE_WEBHOOK_URL=
INTAKE_WEBHOOK_SECRET=
INTAKE_LOCAL_DIR=.intakes

# Supabase browser/server clients.
# These are publishable project values; the service role key below must stay server-only.
NEXT_PUBLIC_SUPABASE_URL=https://nzxedlagqtzadyrmgkhq.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_mAG0Ncil8LY4Ls-LcBUCUw_k_br_aI6

# Supabase intake persistence.
# Keep the service role key in Vercel env vars, not committed .env files.
SUPABASE_REST_URL=https://nzxedlagqtzadyrmgkhq.supabase.co/rest/v1
SUPABASE_INTAKES_TABLE=marketplace_intakes
SUPABASE_SERVICE_ROLE_KEY=
# Optional fallback if you intentionally use RLS policies for inserts instead of service role.
SUPABASE_ANON_KEY=
